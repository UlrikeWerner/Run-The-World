import styled from 'styled-components';

const SiteContent = styled.div`
	flex-grow: 1;
	overflow-y: auto;

	div p {
		margin: 0 0 0 1rem;
	}

	p span {
		font-weight: 800;
	}
`;

export {SiteContent};
