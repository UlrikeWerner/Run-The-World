import styled from 'styled-components';

const StyledActivitiesSection = styled.section`
	margin: 0.5rem 0.8rem 0.5rem 0.8rem;
	padding-top: 0.2rem;
	border-radius: 15px;
	box-shadow: var(--shadow);
	background: var(--dlGreyGradient);
`;

export {StyledActivitiesSection};
