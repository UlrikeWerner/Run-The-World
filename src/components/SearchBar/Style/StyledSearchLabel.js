import styled from 'styled-components';

const StyledSearchLabel = styled.label`
	position: absolute;
	left: 2.5rem;
	top: 0.2rem;
	transition: 1s;

	/*.hide &:before {
		opacity: 0;
		pointer-events: none;
	}

	&:hover {
		color: white;
	}*/
`;

export {StyledSearchLabel};
