import styled from 'styled-components';

const ActivityWrapper = styled.section`
	margin: 0.35rem 0 0;

	h2,
	button {
		margin: 0;
	}

	h3 {
		margin: 0.6rem 0 0;
		font-size: 0.85rem;
	}

	p {
		margin: 0.25rem 0 0;
	}
`;

export {ActivityWrapper};
