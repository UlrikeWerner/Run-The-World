import styled from 'styled-components';

const StyledPercent = styled.p`
	display: block;
	position: relative;
	top: 2.6rem;
	left: -6rem;
	font-weight: 800;
	font-size: 1.5rem;
`;

export {StyledPercent};
